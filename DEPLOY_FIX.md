# 🔧 Исправление проблем с деплоем

## Что было исправлено:

1. ✅ **Добавлена версия Node.js** в `package.json` (engines) и `.nvmrc`
2. ✅ **Улучшена конфигурация Vite** для production сборки
3. ✅ **Обновлен vercel.json** с явными настройками сборки

## Если деплой все еще не работает:

### 1. Проверьте настройки проекта в Vercel

Убедитесь, что в настройках проекта указано:
- **Root Directory**: `frontend`
- **Framework Preset**: `Vite` или `Other`
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`
- **Node.js Version**: `18.x` или выше

### 2. Проверьте логи деплоя в Vercel

1. Зайдите в Vercel Dashboard
2. Откройте ваш проект
3. Перейдите на вкладку "Deployments"
4. Откройте последний деплой
5. Посмотрите логи сборки (Build Logs)

### 3. Типичные ошибки и решения:

#### Ошибка: "Cannot find module"
**Решение**: Убедитесь, что все зависимости указаны в `package.json`

#### Ошибка: "Build failed"
**Решение**: 
- Проверьте, что Node.js версия >= 18
- Убедитесь, что Root Directory = `frontend`

#### Ошибка: "Output directory not found"
**Решение**: Убедитесь, что Output Directory = `dist`

#### Ошибка: "Command failed"
**Решение**: 
- Проверьте логи сборки
- Убедитесь, что `npm install` выполняется успешно

### 4. Ручная проверка сборки локально

Перед деплоем проверьте, что сборка работает локально:

```bash
cd frontend
npm install
npm run build
```

Если сборка проходит успешно, значит проблема в настройках Vercel.

### 5. Пересоздание проекта в Vercel

Если ничего не помогает:
1. Удалите проект в Vercel
2. Создайте новый проект
3. Убедитесь, что Root Directory = `frontend`
4. Деплойте заново

### 6. Проверка через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Перейдите в папку frontend
cd frontend

# Деплой с выводом логов
vercel --prod --debug
```

## Контакты для помощи:

- Vercel Docs: https://vercel.com/docs
- Vercel Support: https://vercel.com/support

